# Configuración del Servidor Central
server.port=5000
server.max_clients=1000

# Configuración de Particionamiento
partitions=3
replication.factor=3

# Rutas de almacenamiento (para nodos)
data.path=/data

# Timeouts (en segundos)
timeout.consulta=5
timeout.transferencia=10